<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <style>
    label {
      font-weight: 700;
      color: #000;
    }

    input {
      font-weight: 600;
    }
  </style>
  </head>
<body>
    <section>
<div class="container">
  <div class="row">
    <div class="col-lg-6 mx-auto mt-4">
      <div class="card shadow">
        <div class="bg-primary">
          <h3 class="text-light text-center"> Edit User (<%= users.firstName %>)</h3>
        </div>
        <div class="card-body p-4">
          <form action="/updateForm/<%= users._id %>" id="add-form" method="post" autocomplete="off">
            <div class="mb-3">
              <label for="firstName"><i class="fa fa-user"></i> firstName</label>
              <input type="text" name="firstName" class="form-control" value="<%= users.firstName %>">
            </div>

            <div class="mb-3">
              <label for="lastName"><i class="fa fa-user"></i> lastName</label>
              <input type="text" name="lastName" class="form-control" value="<%= users.lastName %>">
            </div>

            <div class="mb-3">
                <label for="age"><i class="fa fa-child"></i> age</label>
                <input type="text" name="age" class="form-control" value="<%= users.age %>">
              </div>

            <div class="mb-3">
              <label for="mobileNumber"><i class="fa fa-phone"></i> mobileNumber</label>
              <input type="text" name="mobileNumber" class="form-control" value="<%= users.mobileNumber %>">
            </div>
           <div class="mb-3 d-grid">
              <input type="submit" value="Update Record" onclick="return confirm('Are you sure to update record ?');" class="btn btn-sm btn-primary">
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>
    </section>
</body>
</html>